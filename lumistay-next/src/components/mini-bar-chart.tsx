import type { ReportRow } from "@/lib/types";

export function MiniBarChart({ data, title = "Doanh thu theo tháng" }: { data: ReportRow[]; title?: string }) {
  const max = Math.max(...data.map((item) => item.value), 1);
  return <section className="overflow-hidden rounded-[6px] border border-[#D8D2C7] bg-[#FBFAF7]"><div className="flex items-start justify-between gap-3 border-b border-[#D8D2C7] px-4 py-3.5"><div><h2 className="text-[15px] font-bold text-[#183B35]">{title}</h2><p className="mt-0.5 text-xs text-[#81796D]">Tổng hợp theo kỳ</p></div><span className="rounded-[4px] border border-[#D8D2C7] bg-[#F0EDE7] px-2 py-1 text-[10px] font-semibold text-[#716B61]">triệu VND</span></div><div className="p-4"><div className="flex h-60 items-end gap-2 rounded-[4px] border border-[#E0DAD0] bg-[#F7F4EE] px-3 pb-3 pt-5 sm:gap-3 sm:px-4">{data.map((item) => <div key={item.label} className="flex h-full min-w-0 flex-1 flex-col justify-end gap-2 text-center"><span className="text-[10px] font-semibold tabular-nums text-[#716B61]">{item.value}</span><div className="mx-auto w-full max-w-12 rounded-t-[3px] bg-[#183B35] transition hover:bg-[#9A6A2F]" style={{ height: `${Math.max(10, (item.value / max) * 170)}px` }} title={`${item.label}: ${item.value} triệu VND`} /><span className="truncate text-[10px] text-[#81796D]">{item.label}</span></div>)}{data.length === 0 ? <p className="m-auto text-sm text-[#81796D]">Chưa có dữ liệu doanh thu.</p> : null}</div></div></section>;
}
