import type { ReportRow } from "@/lib/types";

export function SideRankCard({ title, subtitle, rows }: { title: string; subtitle: string; rows: ReportRow[] }) {
  const max = Math.max(...rows.map((row) => row.value), 1);
  return <section className="overflow-hidden rounded-[6px] border border-[#D8D2C7] bg-[#FBFAF7]"><div className="border-b border-[#D8D2C7] px-4 py-3.5"><h2 className="text-[15px] font-bold text-[#183B35]">{title}</h2><p className="mt-0.5 text-xs text-[#81796D]">{subtitle}</p></div><div className="space-y-3.5 p-4">{rows.map((row, index) => <div key={row.label}><div className="mb-1.5 flex items-center justify-between gap-3 text-[12px]"><div className="flex min-w-0 items-center gap-2"><span className="flex h-5 w-5 shrink-0 items-center justify-center rounded-[3px] bg-[#F0EDE7] text-[9px] font-bold text-[#716B61]">{index + 1}</span><span className="truncate font-semibold text-[#4E5852]">{row.label}</span></div><span className="shrink-0 font-bold tabular-nums text-[#183B35]">{row.value}{row.note ? ` ${row.note}` : ""}</span></div><div className="h-1.5 overflow-hidden rounded-full bg-[#E7E1D7]"><div className="h-full rounded-full bg-[#9A6A2F]" style={{ width: `${Math.max(5, (row.value / max) * 100)}%` }} /></div></div>)}{rows.length === 0 ? <p className="py-8 text-center text-sm text-[#81796D]">Chưa có dữ liệu xếp hạng.</p> : null}</div></section>;
}
